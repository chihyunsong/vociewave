<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put },:html => {class: 'form-horizontal'}) do |f| %>
  <%= devise_error_messages! %>
<fieldset>
  <div class="form-group">
 
    <div class="col-lg-5">
       <%= f.label :email,"이메일" %>
      <%= f.text_field :email, disabled:true, :class=>'form-control' %>
       </div>
  </div>

</fieldset>
<% end %>
<h4>비밀번호</h4>
<hr />
비밀번호를 변경하려면 재설정 버튼을 눌러주세요.<br />
 <a href ="/reset_password" data-remote="true" class ="btn btn-default btn-center" style="margin-top:0.5cm"> 재설정</a>
